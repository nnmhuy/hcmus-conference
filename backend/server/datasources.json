{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "conferrence-hcmus": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "${DB_URL}",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "conferrence-hcmus",
    "user": "${DB_USERNAME}",
    "useNewUrlParser": true,
    "connector": "mongodb"
  }
}
