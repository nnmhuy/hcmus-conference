import React from 'react'
// import { useSelector } from 'react-redux';

const ScheduleRecap = () => {
  // const statistic = useSelector(state => state.presentation?.statistic)
  return (
    <div style={{margin: 25, height: 100, background: 'white'}}>
    </div>
  )
}

export default ScheduleRecap